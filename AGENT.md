# Agent 约定

给 Cursor / 自动化助手看的项目约定。改代码或动 Git 前先读这一份。

## Git：小阶段提交，小阶段推送

禁止把多周、多功能的改动攒成一次巨大的 commit / push。

- 一个阶段 = 一件能独立说明、最好能独立验证的事（例如「修排行 dates 查询」「加上空态文案」）。
- 做完一个阶段就单独 commit；需要上线再 push **这一次**（或少数几条紧密相关的）提交，不要等全部做完再整包推上去。
- 一次提交里不要混无关文件。`.env`、密钥、SMTP 授权码、`.next`、构建缓存永远不要进仓库。
- 用户没明确说 commit / push 时，不要自己提交。一旦要求推送，也按小阶段拆开，而不是「工作区里所有改动一次性倒出去」。
