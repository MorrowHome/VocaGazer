version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data  # SQLite 数据持久化
    environment:
      - DATABASE_URL=file:./data/dev.db
    env_file:
      - .env
    restart: unless-stopped
