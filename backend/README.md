# Phase 0 原型（已归档）

这里是半年前验证 B 站搜索 API 的独立脚本，**不再参与运行**。

现行采集在 Next 应用里：

- `src/server/services/bilibili/crawler.ts`
- 定时任务：`src/server/services/scheduler.ts`

若只是想看当年的过滤实验，直接打开这个目录里的脚本即可。目录已不再参与运行，也没有独立的 npm 脚本。

新功能请改主仓库的 `src/`，不要再往这个目录加业务。
