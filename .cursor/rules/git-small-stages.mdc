---
description: Git 必须按小阶段提交和推送，禁止一次推一大包
alwaysApply: true
---

# Git：小阶段提交，小阶段推送

禁止把多周、多功能的改动攒成一次巨大的 commit / push。完整约定见仓库根目录 `AGENT.md`。

- 一个阶段 = 一件能独立说明、最好能独立验证的事。
- 做完一个阶段就单独 commit；需要上线再 push 这一次（或少数紧密相关的）提交。
- `.env`、密钥、SMTP 授权码、`.next` 永远不要进仓库。
- 用户没明确说 commit / push 时不要自己提交；一旦要求推送，也按小阶段拆开。
